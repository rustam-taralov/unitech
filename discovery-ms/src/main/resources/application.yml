server:
  port: ${PORT:8761}

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: ${base-url:http://localhost:8761}/eureka

spring:
  application:
    name: discovery-ms